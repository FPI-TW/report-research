.venv/
__pycache__/
*.pyc
data/extracted/
data/tags/
data/reports/
data/*.json
研報自動匯入/
.uv/
.playwright-mcp/
*.png
# 但保留說明頁截圖（屬 UI 資產、被 help.html 引用，需入版控；其餘 png 仍忽略）
!web/static/help/*.png

# runtime logs / locks / tag backups（執行期產生，非原始碼）
data/*.log
data/*.lock
data/sync_delta_*.txt
data/tags.bak.*/

# 自動產生 / 一次性 artifact（舊的無障礙樹快照）
search-after.yml

# 對外存取邊緣的秘密（隧道 token / Basic Auth 帳密）
deploy/.env
deploy/secrets/

# App 登入帳密 / session 金鑰(本機環境設定)
/.env
