# Cloudflare Tunnel token（從 Zero Trust → Networks → Tunnels 取得）
# 複製本檔為 deploy/.env 並填入實際 token。
# deploy/.env 已被 gitignore，切勿提交真實 token。
TUNNEL_TOKEN=

# 邊緣共享祕密：nginx 會以 `X-Edge-Secret` 注入，App 據此認定可信反向代理，
# 不再只能靠會隨 WSL 重開機漂移的閘道 IP（那個漂移 = 全體外網登入被擋）。
# **必須與 repo 根 .env 的 REPORT_MARK_EDGE_SECRET 逐字相同**；兩邊都留空＝停用
# 此路徑，行為完全退回 REPORT_MARK_TRUSTED_PROXY_CIDRS。
# 產生：python -c "import secrets; print(secrets.token_urlsafe(32))"
# 改值後要 `make edge-reload`（模板只在容器啟動時渲染）。
EDGE_SECRET=
