# 安裝到 /etc/default/report-mark-sync；把主機專屬值留在部署環境，不寫死在 unit。
REPORT_MARK_ROOT=/home/kashionz/projects/report-mark

# 讓 service 能找到 uv（在 ~/.local/bin，不在 systemd 預設 PATH）；依實機調整。sync、audit、freshness、
# backfill、r2-reconcile 都靠它。PR-M 前這裡還帶 nvm 的 node bin（給 claude CLI 用），CLI 移除後不再需要；
# 生產檔裡留著那一段無害，要刪也可以（docs/production_resilience.md「PR-M 部署步驟」）。
SYNC_PATH_EXTRA=/home/kashionz/.local/bin

# 若 uv 不在 PATH，可顯式指定：
# UV=/home/kashionz/.local/bin/uv

# 摘要生成並發數（可選；不設＝沿用 generate_summaries.py 預設 6）。
# Sonnet 額度緊或 CPU 吃緊時可調小，例如：
# SYNC_SUMMARY_WORKERS=4

# ── DB 備份（report-mark-backup.service 與 mount-nas-backup 都讀這個檔）──────
# 要掛哪個 share。**不能用 `投資研究處`**：2026-07-30 實測那組 NAS 帳號對它只有
# 讀取權，第二個掛載點即使是 rw，寫入仍得 EACCES（伺服器端在擋，不是 mount 旗標）。
# 詳細的 errno 對照寫在 deploy/systemd/mount-nas-backup 檔頭。
NAS_BACKUP_UNC=\\192.168.1.100\公用資料夾
REPORT_MARK_BACKUP_MOUNT=/mnt/nas-backup
# 落點目錄。**值含括號，不要在 shell 裡 source 這個檔**（mount-nas-backup 因此
# 改成逐鍵 grep；systemd 的 EnvironmentFile 不做 shell 解析，所以這樣寫是合法的）。
REPORT_MARK_BACKUP_DIR=/mnt/nas-backup/01.會議暫存(會後刪除)/Jacky/report-mark-db
#
# ⚠️ 這個落點是**臨時的**：`01.會議暫存(會後刪除)` 依命名就是會後清掉的暫存區，
#    而備份的內容（qa_log、report_takeaway 等）不可重建。等 NAS 開好不會被清的
#    位置就把上面這行改掉——只改這一個值即可，腳本與 unit 都不必動。
#
# docker CLI 位置。**通常不必設**：scripts/_docker_bin.sh 會實際探 `docker info`
# 逐一試 docker → docker.exe，所以「/usr/bin/docker 存在但連不到 daemon」這種情況
# （Docker Desktop 的 WSL integration 沒開給這個 distro）它自己就跳過了。
# 只有裝在非標準位置時才需要顯式指定：
# DOCKER_BIN=/mnt/c/Program Files/Docker/Docker/resources/bin/docker.exe
#
# 保留份數（不設＝7 日 + 4 週）：
# BACKUP_KEEP_DAILY=7
# BACKUP_KEEP_WEEKLY=4

# ── 抽取器（E1d 切換點，docs/EXTRACTION_REDESIGN.md §9）─────────────────────
# sync 鏈新入庫的研報走哪條抽取路徑：pypdf＝現況、pdfplumber＝版面層。
# E1a–E1c 合併後仍留 pypdf；E1d 這一步把它改成 pdfplumber，同時 enable
# report-mark-backfill.timer 讓既有語料深夜分批跟上。回填腳本不讀這個值（--extractor 顯式給）。
EXTRACTOR=pdfplumber

# ── 私有 R2 物件儲存（sync 入庫上傳原檔、backfill 讀原檔、r2-reconcile 對帳都讀這裡）──
# 與 repo root .env 的同名四行**逐字相同**；OBJECT_STORAGE_MODE 兩邊同步切換
# （local → hybrid 遷移觀察 → r2）。帳號層的 R2_API_TOKEN 程式不讀，不要放。
# OBJECT_STORAGE_MODE=local
# R2_ENDPOINT_URL=https://<account-id>.r2.cloudflarestorage.com
# R2_BUCKET=report-mark
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
