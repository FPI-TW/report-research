# M2 Rerank 接入 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `retrieve_context` 的 `hybrid_search` 之後、`build_context` 之前加入 BGE cross-encoder 重排（`BAAI/bge-reranker-v2-m3`），提升進脈絡片段的相關度（Context Precision），問答＋研報單點共用。

**Architecture:** 新增 `app/services/rerank.py`（延遲載入單例 + `rerank_scores` 模型封裝 + `rerank_scored` 同 tier 內重排整合函式）。`retrieve_context` 加 `rerank_top_m` 參數在檢索後插入；`answer.py`/`report.py` 各自帶 cap（QA 保守、研報較深）。`select_reports`（M0 純函式）完全不改——rerank 只覆蓋 `scored` 的 `fused` 分數，tier 硬性主鍵保留。fail-open。

**Tech Stack:** Python 3.11、FlagEmbedding 1.4.0（`FlagReranker`，已裝、CPU torch）、既有 `retrieve_context`/`build_context`/`select_reports`。測試：`unittest`（`TestCase` / `IsolatedAsyncioTestCase`）跑於 pytest。

## Global Constraints

- **依賴零新增**：`FlagEmbedding` 已裝，`from FlagEmbedding import FlagReranker` 可用；不新增 pyproject 依賴。
- **`select_reports` / `build_context` 選篇邏輯完全不改**（M6 的 MMR 才動）；rerank 只重寫 `scored` 的分數與序。
- **`rerank_top_m=0`（或旗標關）時零行為改變**：`retrieve_context` 不呼叫 rerank，輸出與現況 byte-identical（parity 測鎖定；既有 `test_retrieval_pipeline` 不改仍綠）。
- **fail-open**：模型載入/推論失敗或形狀不符 → `rerank_scored` 回原 `scored`（記 `logger.warning`）；問答/研報照常。
- **排序整合＝同 tier 內重排**：`tier` 為硬性主鍵不變；rerank 分 sigmoid 正規化 [0,1] 覆蓋被重排候選的 `fused`；尾段（top_m 之後）壓縮到嚴格低於最低重排分、保序，守 recall。
- **預設值**：`ASK_RERANK_ENABLED=1`、`ASK_RERANK_CANDIDATES=50`、`REPORT_RERANK_ENABLED=1`、`REPORT_RERANK_CANDIDATES=120`、`RERANK_MODEL=BAAI/bge-reranker-v2-m3`（皆 env 可覆寫，沿用 `app/config.py` 的 frozen dataclass + `_flag` 慣例）。
- **共用工作樹**：只 `git add <明確路徑>`，禁 `git add -A`/`.`；提交前 `git diff --staged --stat` 驗範圍。Conventional Commits + 繁中 scope；commit message 結尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- **分支**：`feat/m2-rerank`（已建、spec 已提交 25867f6，疊於 M1 tip 9a10276）。
- **匯入慣例**：測試檔於檔首 `sys.path.insert(0, str(Path(__file__).resolve().parents[1]))`（沿用 `tests/test_config.py`）。

## 既有 seam（勿改，M2 消費）

- `app.services.retrieval_pipeline.retrieve_context(question, *, k, dense_scan, max_reports, max_passages, max_chars, filters=None, now=None, timer=None) -> tuple[list[Source], str]`——內部 `embed_query_cached → hybrid_search（自開短連線）→ build_context`；`hybrid_search`/`build_context`/`embed_query_cached`/`SessionFactory` 皆為 `retrieval_pipeline` 模組層名（測試 patch where used）。
- `hybrid_search(...) -> list[tuple[int, float, ChunkRow]]`＝`(tier, fused, row)`，`(tier,fused)` 降序；`tier` 2=完整詞組/1=全詞/0=語意；`fused ∈ ~[0,0.999]`。
- `app.services.rows.ChunkRow`（NamedTuple 16 欄；`report_id` 索引 1、`content` 索引 14、`distance` 索引 15）。
- `app.services.answer.build_context / select_reports`——`select_reports` 逐 `(tier,fused,row)` 聚合，排序鍵 `(best_tier, _relevance_band(best_fused), recency, best_fused, report_id)`。
- `app.config.get_settings()` → frozen `Settings`；`app/config.py` 的 `_flag(name, default)` 與 `_load()`。
- 既有 forwarding 測試範式：`tests/test_answer.py::AskRecallConfigTests::test_dense_scan_forwarded_from_ask_path`（patch `rp.hybrid_search` 記錄 kwargs、driveanswer_question、斷言轉發）。

## File Structure

| 檔案 | 責任 |
|---|---|
| `app/config.py`（改） | 加 5 個 rerank 設定欄 + `_load()` 對應 |
| `app/services/rerank.py`（新） | `_get_model` 延遲載入單例；`rerank_scores(query,passages)->list[float]`；`rerank_scored(question,scored,*,top_m,timer)->scored` |
| `app/services/retrieval_pipeline.py`（改） | 模組層 import `rerank_scored`；`retrieve_context` 加 `rerank_top_m=0` 參數與插入點 |
| `app/services/answer.py`（改） | `ASK_RERANK_TOP_M` 模組常數；兩處 `retrieve_context` 傳 `rerank_top_m` |
| `app/services/report.py`（改） | `REPORT_RERANK_TOP_M` 模組常數；`retrieve_context` 傳 `rerank_top_m` |
| `tests/test_config.py`（改） | 新欄預設 parity |
| `tests/test_rerank.py`（新） | `rerank_scores` + `rerank_scored`（核心）單測 |
| `tests/test_retrieval_pipeline.py`（改） | `rerank_top_m=0` parity + `>0` 呼叫/計時 |
| `tests/test_answer.py`（改） | rerank 轉發測試 |
| `tests/test_report.py`（改） | rerank 轉發測試 |
| `eval/baselines/baseline-m2.json`（新，操作性） | rerank 開啟後的基準線 |

---

### Task 1: `app/config.py` — 5 個 rerank 設定

**Files:**
- Modify: `app/config.py`
- Test: `tests/test_config.py`

**Interfaces:**
- Produces: `Settings.ask_rerank_enabled: bool`、`ask_rerank_candidates: int`、`report_rerank_enabled: bool`、`report_rerank_candidates: int`、`rerank_model: str`。

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_config.py` 的 `SettingsDefaultsTests` 類別內加入：

```python
    def test_rerank_defaults(self):
        s = get_settings()
        self.assertEqual(s.ask_rerank_enabled, True)
        self.assertEqual(s.ask_rerank_candidates, 50)
        self.assertEqual(s.report_rerank_enabled, True)
        self.assertEqual(s.report_rerank_candidates, 120)
        self.assertEqual(s.rerank_model, "BAAI/bge-reranker-v2-m3")
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_config.py -v`
Expected: FAIL（`AttributeError: 'Settings' object has no attribute 'ask_rerank_enabled'`）

- [ ] **Step 3: 加欄位到 Settings dataclass**

在 `app/config.py` 的 `Settings` 類別，`report_long_answer_chars: int` 之後加入：

```python
    # rerank.py（M2）
    ask_rerank_enabled: bool
    ask_rerank_candidates: int
    report_rerank_enabled: bool
    report_rerank_candidates: int
    rerank_model: str
```

- [ ] **Step 4: 加預設到 _load()**

在 `app/config.py` 的 `_load()` 回傳的 `Settings(...)`，`report_long_answer_chars=...` 那行之後加入：

```python
        ask_rerank_enabled=_flag("ASK_RERANK_ENABLED", "1"),
        ask_rerank_candidates=int(os.getenv("ASK_RERANK_CANDIDATES", "50")),
        report_rerank_enabled=_flag("REPORT_RERANK_ENABLED", "1"),
        report_rerank_candidates=int(os.getenv("REPORT_RERANK_CANDIDATES", "120")),
        rerank_model=os.getenv("RERANK_MODEL", "BAAI/bge-reranker-v2-m3"),
```

- [ ] **Step 5: 跑測試確認通過**

Run: `uv run pytest tests/test_config.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add app/config.py tests/test_config.py
git commit -m "$(cat <<'EOF'
feat(config): 加 rerank 設定（M2）

ASK_RERANK_ENABLED/CANDIDATES（預設 1/50）、REPORT_RERANK_ENABLED/CANDIDATES
（1/120）、RERANK_MODEL（bge-reranker-v2-m3），沿用 frozen dataclass + _flag。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: `app/services/rerank.py` — `rerank_scores`（模型封裝 + 單例）

**Files:**
- Create: `app/services/rerank.py`
- Test: `tests/test_rerank.py`

**Interfaces:**
- Consumes: `app.config.get_settings().rerank_model`；`FlagEmbedding.FlagReranker`（延遲 import）。
- Produces:
  - `_get_model()`（延遲載入 thread-locked 單例）
  - `rerank_scores(query: str, passages: list[str]) -> list[float]`（相關度 ∈ [0,1]；空 passages → []）

- [ ] **Step 1: 寫失敗測試（新檔）**

Create `tests/test_rerank.py`:

```python
import sys
import unittest
from pathlib import Path
from unittest import mock

REPO_ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(REPO_ROOT))

import app.services.rerank as rr  # noqa: E402


class _FakeModel:
    def __init__(self, scores):
        self._scores = scores
        self.calls = []

    def compute_score(self, pairs, normalize=False):
        self.calls.append((list(pairs), normalize))
        # FlagReranker：單一 pair 回 float、多 pair 回 list
        return self._scores if len(pairs) != 1 else self._scores[0]


class RerankScoresTests(unittest.TestCase):
    def test_returns_score_per_passage(self):
        fake = _FakeModel([0.3, 0.7])
        with mock.patch.object(rr, "_get_model", lambda: fake):
            out = rr.rerank_scores("q", ["a", "b"])
        self.assertEqual(out, [0.3, 0.7])
        # 一次批次、normalize=True（sigmoid [0,1]）
        self.assertEqual(len(fake.calls), 1)
        pairs, normalize = fake.calls[0]
        self.assertEqual(pairs, [("q", "a"), ("q", "b")])
        self.assertTrue(normalize)

    def test_single_passage_float_wrapped_to_list(self):
        fake = _FakeModel([0.42])
        with mock.patch.object(rr, "_get_model", lambda: fake):
            out = rr.rerank_scores("q", ["only"])
        self.assertEqual(out, [0.42])

    def test_empty_passages_returns_empty_without_model(self):
        called = {"n": 0}

        def _boom():
            called["n"] += 1
            raise AssertionError("model should not load for empty passages")

        with mock.patch.object(rr, "_get_model", _boom):
            out = rr.rerank_scores("q", [])
        self.assertEqual(out, [])
        self.assertEqual(called["n"], 0)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_rerank.py -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'app.services.rerank'`）

- [ ] **Step 3: 實作 rerank.py（單例 + rerank_scores）**

Create `app/services/rerank.py`:

```python
"""BGE cross-encoder 重排（CPU）。模型延遲載入並快取為單例（比照 embed.py）。

插在 hybrid_search 之後、build_context 之前（見 retrieval_pipeline）。同 tier 內重排：
rerank 分 sigmoid 正規化 [0,1] 覆蓋被重排候選的 fused、tier 保留；尾段壓縮保 recall。
fail-open：載入/推論失敗回原融合序。
"""

from __future__ import annotations

import logging
import threading

from app.config import get_settings

logger = logging.getLogger(__name__)

_model = None
_lock = threading.Lock()


def _get_model():
    global _model
    if _model is None:
        with _lock:
            if _model is None:
                from FlagEmbedding import FlagReranker

                _model = FlagReranker(get_settings().rerank_model, use_fp16=False)  # CPU
    return _model


def rerank_scores(query: str, passages: list[str]) -> list[float]:
    """回每個 passage 對 query 的相關度 ∈ [0,1]（sigmoid 正規化）。空 → []。

    FlagReranker.compute_score 單一 pair 回 float、多 pair 回 list，統一成 list[float]。
    """
    if not passages:
        return []
    model = _get_model()
    raw = model.compute_score([(query, p) for p in passages], normalize=True)
    if isinstance(raw, (int, float)):
        raw = [raw]
    return [float(s) for s in raw]
```

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_rerank.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5: Commit**

```bash
git add app/services/rerank.py tests/test_rerank.py
git commit -m "$(cat <<'EOF'
feat(檢索): 加 rerank_scores 模型封裝（M2）

延遲載入 thread-locked 單例 FlagReranker(bge-reranker-v2-m3, use_fp16=False)；
rerank_scores 一次批次 compute_score(normalize=True) 回 [0,1]、空 passages 免載模型。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: `app/services/rerank.py` — `rerank_scored`（同 tier 內重排整合，核心）

**Files:**
- Modify: `app/services/rerank.py`
- Test: `tests/test_rerank.py`

**Interfaces:**
- Consumes: `rerank_scores`（Task 2）；`ChunkRow.content`。
- Produces: `rerank_scored(question: str, scored, *, top_m: int, timer=None) -> scored`——回同型 `list[(tier, fused, ChunkRow)]`；前 `top_m` 個以 rerank 分覆蓋 `fused`（tier/row 保留）、尾段壓縮到嚴格低於最低重排分並保序；`top_m<=0`/空/例外/形狀不符 → 回原 `scored`。

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_rerank.py`（`import app.services.rerank as rr` 之後）加入列建構輔助與測試類別：

```python
from datetime import datetime, timezone  # noqa: E402

from app.services.rows import ChunkRow  # noqa: E402


def _row(rid, content):
    vals = [None] * 16
    vals[1] = rid       # report_id
    vals[14] = content  # content
    vals[15] = 0.0      # distance
    return ChunkRow._make(vals)


def _scored(*items):
    """items 為 (tier, fused, report_id, content)。"""
    return [(t, f, _row(rid, c)) for (t, f, rid, c) in items]


class RerankScoredTests(unittest.TestCase):
    def test_overwrites_fused_keeps_tier_and_row(self):
        scored = _scored((0, 0.9, "A", "aa"), (0, 0.8, "B", "bb"), (0, 0.7, "C", "cc"))
        rows = [r for (_t, _f, r) in scored]
        with mock.patch.object(rr, "rerank_scores", lambda q, ps: [0.2, 0.5, 0.9]):
            out = rr.rerank_scored("q", scored, top_m=3)
        self.assertEqual([t for (t, _f, _r) in out], [0, 0, 0])       # tier 保留
        self.assertEqual([f for (_t, f, _r) in out], [0.2, 0.5, 0.9]) # fused=rerank 分
        self.assertEqual([r for (_t, _f, r) in out], rows)            # row 物件不變

    def test_passes_content_to_rerank(self):
        scored = _scored((0, 0.9, "A", "內容一"), (0, 0.8, "B", "內容二"))
        seen = {}
        def _fake(q, ps):
            seen["q"], seen["ps"] = q, ps
            return [0.1, 0.2]
        with mock.patch.object(rr, "rerank_scores", _fake):
            rr.rerank_scored("我的問題", scored, top_m=2)
        self.assertEqual(seen["q"], "我的問題")
        self.assertEqual(seen["ps"], ["內容一", "內容二"])

    def test_tail_compressed_below_min_rerank_preserves_order(self):
        scored = _scored(
            (0, 0.9, "A", "a"), (0, 0.8, "B", "b"),
            (0, 0.7, "C", "c"), (0, 0.6, "D", "d"),
        )
        with mock.patch.object(rr, "rerank_scores", lambda q, ps: [0.4, 0.6]):
            out = rr.rerank_scored("q", scored, top_m=2)
        head, tail = out[:2], out[2:]
        self.assertEqual([f for (_t, f, _r) in head], [0.4, 0.6])      # 重排分覆蓋
        self.assertEqual([r.content for (_t, _f, r) in head], ["a", "b"])
        min_rr = 0.4
        self.assertTrue(all(f < min_rr for (_t, f, _r) in tail))       # 尾段嚴格低於最低重排分
        self.assertEqual([r.content for (_t, _f, r) in tail], ["c", "d"])  # 保相對序
        self.assertGreaterEqual(tail[0][1], tail[1][1])                # C(原0.7) >= D(原0.6)

    def test_top_m_truncates_reranked_head(self):
        scored = _scored((0, 0.9, "A", "a"), (0, 0.8, "B", "b"), (0, 0.7, "C", "c"))
        calls = {}
        def _fake(q, ps):
            calls["ps"] = ps
            return [0.5]  # 僅 1 個（top_m=1）
        with mock.patch.object(rr, "rerank_scores", _fake):
            out = rr.rerank_scored("q", scored, top_m=1)
        self.assertEqual(calls["ps"], ["a"])          # 只重排前 1 個
        self.assertEqual(out[0][1], 0.5)              # A 用重排分
        self.assertEqual([r.content for (_t, _f, r) in out], ["a", "b", "c"])  # 全部保留（保 recall）

    def test_tier_preserved_on_all_items(self):
        scored = _scored((0, 0.9, "A", "a"), (1, 0.5, "B", "b"))  # A tier0, B tier1
        with mock.patch.object(rr, "rerank_scores", lambda q, ps: [0.2]):
            out = rr.rerank_scored("q", scored, top_m=1)          # head=[A], tail=[B]
        self.assertEqual(out[0][0], 0)  # A tier0
        self.assertEqual(out[1][0], 1)  # B tier1 保留

    def test_fail_open_on_rerank_exception_returns_original(self):
        scored = _scored((0, 0.9, "A", "a"), (0, 0.8, "B", "b"))
        def _boom(q, ps):
            raise RuntimeError("model down")
        with mock.patch.object(rr, "rerank_scores", _boom):
            out = rr.rerank_scored("q", scored, top_m=2)
        self.assertIs(out, scored)  # 逐字節不變（同物件）

    def test_shape_mismatch_fails_open(self):
        scored = _scored((0, 0.9, "A", "a"), (0, 0.8, "B", "b"))
        with mock.patch.object(rr, "rerank_scores", lambda q, ps: [0.5]):  # 長度不符
            out = rr.rerank_scored("q", scored, top_m=2)
        self.assertIs(out, scored)

    def test_zero_top_m_and_empty_are_passthrough(self):
        scored = _scored((0, 0.9, "A", "a"))
        self.assertIs(rr.rerank_scored("q", scored, top_m=0), scored)
        self.assertEqual(rr.rerank_scored("q", [], top_m=5), [])

    def test_tier_priority_preserved_through_build_context(self):
        # 端到端：tier1 字面命中即使 rerank 分低，仍排在 tier0 語意（rerank 分高）之上
        from app.services.answer import build_context
        scored = _scored((1, 0.5, "lit", "字面"), (0, 0.9, "sem", "語意"))
        with mock.patch.object(rr, "rerank_scores", lambda q, ps: [0.1, 0.95]):
            reranked = rr.rerank_scored("q", scored, top_m=2)
        sources, _ = build_context(reranked, now=datetime(2026, 6, 17, tzinfo=timezone.utc))
        self.assertEqual(sources[0].report_id, "lit")  # tier 硬性優先於 rerank 分
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_rerank.py::RerankScoredTests -v`
Expected: FAIL（`AttributeError: module 'app.services.rerank' has no attribute 'rerank_scored'`）

- [ ] **Step 3: 實作 rerank_scored**

在 `app/services/rerank.py` 檔尾加入：

```python
def rerank_scored(question, scored, *, top_m, timer=None):
    """對 scored 前 top_m 個（依現行序）重排：rerank 分覆蓋 fused、tier/row 保留；
    尾段壓縮到嚴格低於最低重排分並保相對序（守 recall、同 tier 內不反超）。
    任何例外/退化/形狀不符 → 回原 scored（fail-open）。
    """
    if not scored or top_m <= 0:
        return scored
    try:
        head = scored[:top_m]
        tail = scored[top_m:]
        scores = rerank_scores(question, [row.content for (_t, _f, row) in head])
        if len(scores) != len(head):
            return scored  # 形狀不符：fail-open
        reranked = [(tier, scores[i], row) for i, (tier, _f, row) in enumerate(head)]
        if tail:
            min_rr = min(scores)
            ceiling = min_rr * 0.99  # 嚴格低於最低重排分
            tail_fused = [f for (_t, f, _r) in tail]
            lo, hi = min(tail_fused), max(tail_fused)
            span = hi - lo
            tail = [
                (tier, (ceiling if span == 0 else ceiling * (f - lo) / span), row)
                for (tier, f, row) in tail
            ]
        if timer is not None:
            timer.mark("rerank")
        return reranked + tail
    except Exception:
        logger.warning("rerank failed, fall back to fused order", exc_info=True)
        return scored
```

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_rerank.py -v`
Expected: PASS（3 + 9 = 12 passed）

- [ ] **Step 5: Commit**

```bash
git add app/services/rerank.py tests/test_rerank.py
git commit -m "$(cat <<'EOF'
feat(檢索): 加 rerank_scored 同 tier 內重排整合（M2）

前 top_m 候選以 rerank 分覆蓋 fused、tier/row 保留；尾段壓縮到嚴格低於最低重排分
並保序（守 recall、同 tier 內不反超）；top_m<=0/空/例外/形狀不符皆 fail-open 回原序。
端到端測驗 tier 硬性優先於 rerank 分（經 build_context）。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: `retrieve_context` — `rerank_top_m` 插入點

**Files:**
- Modify: `app/services/retrieval_pipeline.py`
- Test: `tests/test_retrieval_pipeline.py`

**Interfaces:**
- Consumes: `app.services.rerank.rerank_scored`（Task 3）。
- Produces: `retrieve_context(..., rerank_top_m: int = 0)`——`rerank_top_m>0` 時在 `hybrid_search` 之後、`build_context` 之前呼叫 `rerank_scored(question, scored, top_m=rerank_top_m, timer=timer)`；`=0` 時零行為改變。patch target＝`retrieval_pipeline.rerank_scored`。

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_retrieval_pipeline.py` 的 `RetrieveContextTests` 類別內加入（沿用既有 `_Session`/`_Timer`/mock 範式）：

```python
    async def test_rerank_top_m_zero_skips_rerank(self):
        import app.services.retrieval_pipeline as rp
        from unittest import mock

        called = {"n": 0}

        class _Session:
            async def __aenter__(self): return self
            async def __aexit__(self, *a): return False

        async def _fake_hybrid(session, q, vec, **kw):
            return [(0, 0.5, "row")]

        def _fake_build(scored, **kw):
            return (["S"], "CTX")

        def _spy_rerank(question, scored, *, top_m, timer=None):
            called["n"] += 1
            return scored

        with mock.patch.object(rp, "embed_query_cached", lambda q: [0.1]), \
             mock.patch.object(rp, "SessionFactory", lambda: _Session()), \
             mock.patch.object(rp, "hybrid_search", _fake_hybrid), \
             mock.patch.object(rp, "build_context", _fake_build), \
             mock.patch.object(rp, "rerank_scored", _spy_rerank):
            out = await rp.retrieve_context(
                "q", k=15, dense_scan=400, max_reports=15,
                max_passages=4, max_chars=20000, rerank_top_m=0,
            )
        self.assertEqual(out, (["S"], "CTX"))
        self.assertEqual(called["n"], 0)  # rerank_top_m=0 → 不呼叫

    async def test_rerank_top_m_positive_calls_rerank_and_marks_timer(self):
        import app.services.retrieval_pipeline as rp
        from unittest import mock

        seen = {}

        class _Session:
            async def __aenter__(self): return self
            async def __aexit__(self, *a): return False

        class _Timer:
            def __init__(self): self.marks = []
            def mark(self, name): self.marks.append(name)

        async def _fake_hybrid(session, q, vec, **kw):
            return [(0, 0.5, "row")]

        def _fake_build(scored, **kw):
            seen["build_scored"] = scored
            return (["S"], "CTX")

        def _fake_rerank(question, scored, *, top_m, timer=None):
            seen["top_m"] = top_m
            if timer is not None:
                timer.mark("rerank")
            return [(0, 0.99, "reranked")]

        t = _Timer()
        with mock.patch.object(rp, "embed_query_cached", lambda q: [0.1]), \
             mock.patch.object(rp, "SessionFactory", lambda: _Session()), \
             mock.patch.object(rp, "hybrid_search", _fake_hybrid), \
             mock.patch.object(rp, "build_context", _fake_build), \
             mock.patch.object(rp, "rerank_scored", _fake_rerank):
            await rp.retrieve_context(
                "q", k=15, dense_scan=400, max_reports=15,
                max_passages=4, max_chars=20000, rerank_top_m=50, timer=t,
            )
        self.assertEqual(seen["top_m"], 50)
        self.assertEqual(seen["build_scored"], [(0, 0.99, "reranked")])  # 重排結果進 build_context
        self.assertIn("rerank", t.marks)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_retrieval_pipeline.py -v`
Expected: FAIL（`TypeError: retrieve_context() got an unexpected keyword argument 'rerank_top_m'`，且 `AttributeError` on `rp.rerank_scored`）

- [ ] **Step 3: 加模組層 import**

在 `app/services/retrieval_pipeline.py` 的 import 區（`from app.services.retrieval import hybrid_search` 之後）加入：

```python
from app.services.rerank import rerank_scored
```

- [ ] **Step 4: 加參數與插入點**

把 `retrieve_context` 的簽名與檢索後段改為（新增 `rerank_top_m=0` 參數與 rerank 插入）：

```python
async def retrieve_context(
    question: str,
    *,
    k: int,
    dense_scan: int,
    max_reports: int,
    max_passages: int,
    max_chars: int,
    filters: dict | None = None,
    now=None,
    timer=None,
    rerank_top_m: int = 0,
) -> tuple[list[Source], str]:
    """回 (sources, context)。timer 給定時記 embed/retrieve/rerank 各段耗時。
    rerank_top_m>0 時在檢索後、選篇前插入 cross-encoder 重排（fail-open）。"""
    filters = filters or {}
    qvec = await asyncio.to_thread(embed_query_cached, question)
    if timer is not None:
        timer.mark("embed")
    async with SessionFactory() as session:  # 短連線：檢索完即釋放
        scored = await hybrid_search(
            session, question, qvec, k=k, dense_scan=dense_scan, **filters
        )
    if timer is not None:
        timer.mark("retrieve")
    if rerank_top_m > 0:
        scored = rerank_scored(question, scored, top_m=rerank_top_m, timer=timer)
    return build_context(
        scored,
        max_reports=max_reports,
        max_passages=max_passages,
        max_chars=max_chars,
        now=now,
    )
```

- [ ] **Step 5: 跑測試確認通過（含既有 parity 測不改仍綠）**

Run: `uv run pytest tests/test_retrieval_pipeline.py -v`
Expected: PASS（既有 `test_wires_embed_search_buildcontext_and_marks_timer`＝3 個 mark 中無 rerank、不傳 rerank_top_m 預設 0 不呼叫 rerank；+2 新測）

- [ ] **Step 6: Commit**

```bash
git add app/services/retrieval_pipeline.py tests/test_retrieval_pipeline.py
git commit -m "$(cat <<'EOF'
feat(檢索): retrieve_context 加 rerank_top_m 插入點（M2）

模組層 import rerank_scored；rerank_top_m>0 時在 hybrid_search 之後、build_context
之前插入重排並記 rerank 段耗時；=0（預設）零行為改變（既有 parity 測不改仍綠）。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: `answer.py` + `report.py` 接線（各自帶 cap）

**Files:**
- Modify: `app/services/answer.py`
- Modify: `app/services/report.py`
- Test: `tests/test_answer.py`, `tests/test_report.py`

**Interfaces:**
- Consumes: `get_settings()` 的 rerank 欄（Task 1）；`retrieve_context` 的 `rerank_top_m`（Task 4）。
- Produces: `answer.ASK_RERANK_TOP_M: int`、`report.REPORT_RERANK_TOP_M: int`（旗標關→0）；三個 `retrieve_context` 呼叫傳 `rerank_top_m`。

- [ ] **Step 1: 寫 answer.py 轉發失敗測試**

在 `tests/test_answer.py` 的 `AskRecallConfigTests` 類別內加入（沿用既有 `make_row`/`_FakeSession`/mock 範式）：

```python
    async def test_rerank_top_m_forwarded_from_ask_path(self):
        from app.services import answer as ans
        import app.services.retrieval_pipeline as rp

        captured = {}

        async def recording_search(session, q, qvec, **k):
            return [(0, 0.80, make_row("r1", "x.pdf", "TW", "內容。", date(2026, 6, 1)))]

        def recording_rerank(question, scored, *, top_m, timer=None):
            captured["top_m"] = top_m
            return scored

        async def fake_stream(*a, **k):
            yield "答案[1]"

        async def fake_intent(question, **k):
            return True

        orig = (
            rp.hybrid_search, rp.embed_query_cached, rp.rerank_scored,
            ans.stream_completion, rp.SessionFactory, ans.SessionFactory,
            ans.classify_intent,
        )
        rp.hybrid_search = recording_search
        rp.embed_query_cached = lambda q: [0.0]
        rp.rerank_scored = recording_rerank
        ans.stream_completion = fake_stream
        rp.SessionFactory = lambda: _FakeSession()
        ans.SessionFactory = lambda: _FakeSession()
        ans.classify_intent = fake_intent
        try:
            _ = [e async for e in ans.answer_question("台積電展望")]
        finally:
            (
                rp.hybrid_search, rp.embed_query_cached, rp.rerank_scored,
                ans.stream_completion, rp.SessionFactory, ans.SessionFactory,
                ans.classify_intent,
            ) = orig

        self.assertEqual(captured.get("top_m"), ans.ASK_RERANK_TOP_M)
        self.assertEqual(ans.ASK_RERANK_TOP_M, 50)  # 預設啟用
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_answer.py::AskRecallConfigTests::test_rerank_top_m_forwarded_from_ask_path -v`
Expected: FAIL（`AttributeError: module 'app.services.answer' has no attribute 'ASK_RERANK_TOP_M'`）

- [ ] **Step 3: answer.py 加常數 + 傳參**

在 `app/services/answer.py` 的 `ASK_DENSE_SCAN = _S.ask_dense_scan` 之後加入：

```python
# rerank（M2）：問答路徑保守候選上限；旗標關時 0＝不重排
ASK_RERANK_TOP_M = _S.ask_rerank_candidates if _S.ask_rerank_enabled else 0
```

把兩處 `retrieve_context` 呼叫（`if turns:` 分支與 `else` 分支）各加 `rerank_top_m=ASK_RERANK_TOP_M`：

```python
    if turns:
        sources, context = await retrieve_context(
            standalone_query, k=k, dense_scan=ASK_DENSE_SCAN,
            max_reports=MAX_REPORTS, max_passages=MAX_PASSAGES_PER_REPORT,
            max_chars=MAX_CONTEXT_CHARS, filters=filters, timer=timer,
            rerank_top_m=ASK_RERANK_TOP_M,
        )
    else:
        intent_task = asyncio.create_task(classify_intent(question))
        try:
            sources, context = await retrieve_context(
                question, k=k, dense_scan=ASK_DENSE_SCAN,
                max_reports=MAX_REPORTS, max_passages=MAX_PASSAGES_PER_REPORT,
                max_chars=MAX_CONTEXT_CHARS, filters=filters, timer=timer,
                rerank_top_m=ASK_RERANK_TOP_M,
            )
```

- [ ] **Step 4: 跑 answer 轉發測試確認通過**

Run: `uv run pytest tests/test_answer.py::AskRecallConfigTests -v`
Expected: PASS

- [ ] **Step 5: 寫 report.py 轉發失敗測試**

先看 `tests/test_report.py` 現有 mock 範式（M0 Task 4 已把三內層函式併為 `rpt.retrieve_context` mock）。在 `tests/test_report.py` 加入一個轉發測試（patch `report` 模組的 `retrieve_context` 捕捉 kwarg）：

```python
    async def test_rerank_top_m_forwarded_from_report_path(self):
        from app.services import report as rpt

        captured = {}

        async def recording_retrieve(question, **kw):
            captured.update(kw)
            return ([], "")

        orig = rpt.retrieve_context
        rpt.retrieve_context = recording_retrieve
        try:
            # 脈絡空且網搜關 → 早退，足以捕捉 retrieve_context 的 kwargs
            orig_web = rpt.REPORT_ENABLE_WEB
            rpt.REPORT_ENABLE_WEB = False
            try:
                _ = [e async for e in rpt.generate_report("台積電深度研報")]
            finally:
                rpt.REPORT_ENABLE_WEB = orig_web
        finally:
            rpt.retrieve_context = orig

        self.assertEqual(captured.get("rerank_top_m"), rpt.REPORT_RERANK_TOP_M)
        self.assertEqual(rpt.REPORT_RERANK_TOP_M, 120)  # 預設啟用
```

> 註：把此方法加進 `tests/test_report.py` 的 `GenerateReportTests(unittest.IsolatedAsyncioTestCase)` 類別（與 `test_event_sequence_and_done_payload` 等同一類別）。此測試僅 patch `rpt.retrieve_context`（module 層屬性，report.py 於 :29 module 層 import）並暫關 `rpt.REPORT_ENABLE_WEB`——context 空 + web 關時 `generate_report` 於 :219 早退（yield error+return），不觸及 `stream_completion`/`render_report_pdf`/`persist`，故無需 patch 它們。`recording_retrieve(question, **kw)` 簽名對齊既有 `fake_retrieve_context(question, **k)`。

- [ ] **Step 6: 跑測試確認失敗**

Run: `uv run pytest tests/test_report.py -k rerank_top_m_forwarded -v`
Expected: FAIL（`AttributeError: module 'app.services.report' has no attribute 'REPORT_RERANK_TOP_M'`）

- [ ] **Step 7: report.py 加常數 + 傳參**

在 `app/services/report.py` 的 `REPORT_THIN_COVERAGE = _S.report_thin_coverage`（:50）之後加入（`_S = get_settings()` 已於 :33 定義）：

```python
# rerank（M2）：研報路徑較深候選上限；旗標關時 0＝不重排
REPORT_RERANK_TOP_M = _S.report_rerank_candidates if _S.report_rerank_enabled else 0
```

把 `retrieve_context`（:208）呼叫加 `rerank_top_m=REPORT_RERANK_TOP_M`：

```python
    sources, context = await retrieve_context(
        question,
        k=REPORT_DEEP_K,
        dense_scan=ASK_DENSE_SCAN,
        max_reports=REPORT_MAX_REPORTS,
        max_passages=REPORT_MAX_PASSAGES,
        max_chars=REPORT_MAX_CONTEXT_CHARS,
        filters=filters,
        rerank_top_m=REPORT_RERANK_TOP_M,
    )
```

- [ ] **Step 8: 跑 report 轉發測試確認通過**

Run: `uv run pytest tests/test_report.py -k rerank_top_m_forwarded -v`
Expected: PASS

- [ ] **Step 9: 全套回歸**

Run: `uv run pytest -q`
Expected: PASS（既有 433 + M2 新增；零既有測試異動或失敗）

- [ ] **Step 10: Commit**

```bash
git add app/services/answer.py app/services/report.py tests/test_answer.py tests/test_report.py
git commit -m "$(cat <<'EOF'
feat(檢索/問答): 接線 rerank 至問答與研報路徑（M2）

answer.py ASK_RERANK_TOP_M（旗標關→0）傳兩處 retrieve_context；report.py
REPORT_RERANK_TOP_M（120）傳研報路徑。轉發測試沿用 dense_scan 範式驗 kwarg。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: 產生 rerank 基準線 `eval/baselines/baseline-m2.json`（操作性，需 live infra）

> **注意：本 task 非 TDD 單元任務**——要真跑 DB + claude CLI + BGE-M3 + bge-reranker-v2-m3 產出 rerank 後基準線並與 M0 對比。若 subagent 環境無法起 DB/載模型，改由編排者（主 session）或使用者手動執行。單元正確性已由 Task 1–5 保證。M1 基準線產生時 Anthropic API 曾間歇 529，如遇之沿用「多次序列跑合併每題成功結果」（見 [[report-qa-m1-eval-baseline-2026-07-09]]）。

**Files:**
- Create: `eval/baselines/baseline-m2.json`

- [ ] **Step 1: 預暖 reranker 模型（首載 ~600MB）**

Run: `uv run python -c "from app.services.rerank import rerank_scores; print(rerank_scores('台積電展望', ['台積電先進製程營運展望正向', '天氣預報']))"`
Expected: 印出兩個 [0,1] 分數，第一個明顯高於第二個（相關 vs 不相關）；首次會下載 bge-reranker-v2-m3（~600MB）。

- [ ] **Step 2: 跑 rerank 後基準線（同 M1 題集、rerank 預設開）**

Run: `uv run python eval/run_ragas.py --dataset eval/ragas_questions.json --out eval/baselines/baseline-m2.json --judge-model claude-haiku-4-5 --concurrency 1`
Expected: 印出摘要；`errors=0`（如遇 529 fail-open 則多跑合併）。rerank 經 answer.py 預設啟用 → 檢索走重排。

- [ ] **Step 3: 對比 M0 基準線**

Run: `uv run python -c "import json; m0=json.load(open('eval/baselines/baseline-m0.json'))['summary']; m2=json.load(open('eval/baselines/baseline-m2.json'))['summary']; print('CP  %.3f -> %.3f' % (m0['context_precision'], m2['context_precision'])); print('F   %.3f -> %.3f' % (m0['faithfulness'], m2['faithfulness'])); print('AR  %.3f -> %.3f' % (m0['answer_relevancy'], m2['answer_relevancy']))"`
Expected: **Context Precision 上升**、Faithfulness 不降。若未升，記錄並於審查討論是否需調 `ASK_RELEVANCE_FLOOR`/`ASK_RELEVANCE_BAND`（env）或候選上限。

- [ ] **Step 4: 檢索召回不降（既有關鍵字 harness）**

Run: `ASK_RERANK_ENABLED=0 uv run python scripts/eval_retrieval.py --queryset eval/queryset.json --json > /tmp/recall-off.json; uv run python scripts/eval_retrieval.py --queryset eval/queryset.json --json > /tmp/recall-on.json; uv run python -c "import json; a=json.load(open('/tmp/recall-off.json')); b=json.load(open('/tmp/recall-on.json')); print('hit_rate', a.get('hit_rate'), '->', b.get('hit_rate'))"`
Expected: hit_rate 不下降（rerank 不丟候選、尾段保留）。（`scripts/eval_retrieval.py` 跑到 build_context 為止；rerank 經 retrieve_context 不在其路徑——故此步主要驗「rerank 開關不破壞既有召回骨架」；若 eval_retrieval 不經 rerank，記錄為「召回骨架未受影響」。）

- [ ] **Step 5: 人工抽驗 + Commit**

人工抽看 2-3 題 rerank 前後 top 片段是否更相關（`baseline-m2.json` 的 `cases`）。

```bash
git add eval/baselines/baseline-m2.json
git commit -m "$(cat <<'EOF'
chore(eval): 提交 M2 rerank 基準線 baseline-m2

同 M1 題集、rerank 預設開的基準線；與 baseline-m0 對比量 Context Precision 增益。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

## Self-Review

**1. Spec coverage：**
- `rerank.py`（rerank_scores 單例 + rerank_scored 同 tier 內重排/尾段壓縮/fail-open）→ Task 2、3。✅
- `retrieve_context` 加 rerank_top_m 插入點、=0 byte-identical → Task 4。✅
- answer.py/report.py 各自帶 cap 接線 → Task 5。✅
- config 5 欄（enabled×2/candidates×2/model）→ Task 1。✅
- select_reports 不改 → 全計畫無改動 select_reports（Task 3 端到端測經 build_context 驗 tier 優先）。✅
- 依賴零新增 → 用既有 FlagEmbedding（Task 2）。✅
- fail-open（載入/推論失敗/形狀不符/旗標關）→ Task 3 多測 + Task 4 rerank_top_m=0。✅
- 尾段壓縮保 recall、同 tier 內不反超、跨 tier tier 主鍵 → Task 3 測。✅
- rerank_ms 計時 → Task 3 timer.mark("rerank") + Task 4 驗 timer。✅
- eval 驗 CP↑/F 不降/recall 不降/延遲 → Task 6。✅
- 模型預暖（~600MB）→ Task 6 Step 1。✅

**2. Placeholder scan：** 無 TBD/TODO；每個 code step 含完整程式碼；每個 test step 含實際測試碼與預期。Task 5 對 report.py 的 `_S`/類別名以「沿用該檔既有慣例」明確界定（非佔位——answer.py 用 `_S`，report.py 同 M0 慣例）。Task 6 明標操作性。✅

**3. Type consistency：**
- `rerank_scores(query, passages) -> list[float]`（Task 2）↔ `rerank_scored` 呼叫（Task 3）一致。✅
- `rerank_scored(question, scored, *, top_m, timer=None) -> scored`（Task 3）↔ `retrieve_context` 呼叫（Task 4）一致。✅
- `retrieve_context(..., rerank_top_m=0)`（Task 4）↔ answer.py/report.py 傳 `rerank_top_m=`（Task 5）一致。✅
- config 欄名 `ask_rerank_enabled/ask_rerank_candidates/report_rerank_enabled/report_rerank_candidates/rerank_model`（Task 1）↔ Task 2（rerank_model）/Task 5（ask_/report_rerank_*）一致。✅
- patch target `retrieval_pipeline.rerank_scored`（模組層 import）↔ Task 4 測一致。✅

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-08-m2-rerank.md`. Two execution options:

**1. Subagent-Driven (recommended)** — 每 task 派新 subagent（test-first）+ task 間兩階段審查（Task 1–5 為 TDD 單元；Task 6 操作性由編排者/使用者跑 live infra）。

**2. Inline Execution** — 於本 session 以 executing-plans 批次執行、含 checkpoint 審查。
